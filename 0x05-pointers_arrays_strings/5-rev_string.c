#include "main.h"
#include <stdio.h>

/**
 * rev_string -  function that reverses a string
 * @s: reverse string
 * Return: reverse string
 */
void rev_string(char *s)
{
	int s = *strlen;

	while (*(str + len) '\0')
		putchar (strlen);
	strlen--
}
