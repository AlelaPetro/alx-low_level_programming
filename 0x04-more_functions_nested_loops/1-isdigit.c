#include "main.h"
/**
 * _isdigit - function that checks for a digit (0 through 9).
 * @c: tested character
 * Return: 1, otherwise 0
 */
int _isdigit(int c)
{
	if ((c >= 48) && (c <= 57))
	{
		return (0);
	}

	return (0);
}
